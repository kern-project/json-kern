[package]
name = "json"
version = "0.1.0"
kern = "0.7.5"
publish = true
description = "JSON parsing and document utilities for Kern"
readme = "README.md"
license = "MIT"
authors = ["softfault <givenyoung46@gmail.com>"]
repository = "https://github.com/softfault/json-kern.git"

[lib]
root = "src/lib.rn"

[test]
roots = [
    "tests/smoke.rn",
    "tests/api_compile.rn",
]
